[package]
name = "orangebox-domain"
version.workspace = true
edition.workspace = true
license.workspace = true

# The domain layer has zero dependencies by design (Clean Architecture
# dependency rule): entities must not know about storage, parsing, or I/O.
[dependencies]
